/*
                    INDEX
1. Using postfix (i++/--) and prefix (++/--i) together
*/

/*
* 1. Using postfix (i++/--) and prefix (++/--i) together

Precedence of postfix (i++/--) > prefix (++/--i)
? Associativity of postfix (i++/--) is L to R.
? Associativity of prefix (++/--i) is R to L.

! NOTE: Associativity means if two operations using the same
! operator are taking place in an expression, if the left one would
! take place first or the right one.

The precedence of prefix ++/-- and * is the same 
with the right to left associativity.

*/